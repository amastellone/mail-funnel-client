<%= render 'layouts/navbar' %>

<h3><%= @app.name %></h3>

<div class="container">
	<ul class="list-group">
		<% @emails.each do |e| %>
				<li class="list-group-item">
					<b><%= e.name %></b> - <%= e.email_address %>
				</li>
		<% end %>
	</ul>
	
</div>